import { AnyAction } from 'redux';

const initialState = {};

const user = (state = initialState, action: AnyAction) => {
  switch (action.type) {
    default: return state;
  }
};

export default user;
